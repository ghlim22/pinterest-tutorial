name: pingpong

services:
  backend:
    build:  ./backend
    container_name: backend
    networks:
      - intra
    ports:
      - '8000:8000'
    restart: on-failure

networks:
  intra:
    driver: bridge
